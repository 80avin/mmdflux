You are implementing plan 0048-subgraph-maturity for the mmdflux project.

## Your Working Directory

You are in a git worktree at `/Users/kevin/src/mmdflux/worktrees/subgraph` on branch `plan/0048-subgraph-maturity`. The main repo is at `/Users/kevin/src/mmdflux`. The `.gumbo/` directory with plans is accessible from the main repo.

## Your Plan

Read the full plan at `/Users/kevin/src/mmdflux/.gumbo/plans/0048-subgraph-maturity/task-list.md`. The detailed task files are in `/Users/kevin/src/mmdflux/.gumbo/plans/0048-subgraph-maturity/tasks/`.

This plan has 18 tasks across 5 phases:
- Phase 1: Subgraph-as-node edge resolution
- Phase 2: Multi-word subgraph titles and numeric IDs
- Phase 3: Direction override architecture evaluation (prototyping two approaches)
- Phase 4: Direction override implementation
- Phase 5: Integration tests

## Key Decisions

- **Phase 3 prototyping**: Tasks 3.2 and 3.3 should each be prototyped in sub-worktrees (create them off your branch). Task 3.4 compares them. The plan recommends a compound-aware hybrid approach (3.3) but both must be prototyped.
- **Subgraph-as-node edges**: resolve to a child node using `find_non_cluster_child()` during `build_diagram()`
- **Multi-word titles**: extend grammar to support `subgraph id["Title with spaces"]`
- **Anonymous subgraphs**: auto-generate IDs like `subgraph_0`, `subgraph_1`

## How to Work

For each task in order:
1. Read the task file from `tasks/` for detailed implementation guidance (code snippets, file paths, acceptance criteria)
2. **Red**: Write failing test(s) first. Run `cargo test` to confirm they fail.
3. **Green**: Write minimum code to pass. Run `cargo test` to confirm.
4. **Refactor**: Clean up while keeping tests green.
5. Run `cargo clippy` and `cargo fmt`
6. Commit with a descriptive message (do NOT add Claude as co-author)
7. Update the task-list.md checkbox: `- [ ]` → `- [x]`
8. Update `.plan-state.json` progress (increment completed count, set current_task)
9. If you discover anything unexpected, record it in `findings/`

## Phase 3 Special Instructions

For tasks 3.2 and 3.3, create sub-worktrees off your current branch:
```bash
git worktree add ../subgraph-approach-a -b prototype/recursive-sublayout
git worktree add ../subgraph-approach-c -b prototype/compound-hybrid
```
Work in each sub-worktree for the respective prototype, then compare in task 3.4. After choosing an approach, merge the winning branch and remove the sub-worktrees.

## Source References

- mmdflux source: `/Users/kevin/src/mmdflux/src/`
- Mermaid reference: `~/src/mermaid/packages/mermaid/src/diagrams/flowchart/`
- dagre reference: `~/src/dagre/lib/`
- Research: `/Users/kevin/src/mmdflux/.gumbo/research/0037-mermaid-feature-delta/`

## Important

- Run all cargo commands in THIS worktree directory, not the main repo
- Commit after EACH task (not each phase)
- Follow strict TDD — no implementation before tests
- Keep commits clean and focused
- Phase 3 is the most architecturally complex — read each task file carefully before prototyping
